<UserControl x:Class="Course_Pilot.Views.CourseCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Course_Pilot.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Border 
            Background="Beige"
            BorderThickness="1"
            CornerRadius="8"
            Padding="15"
            Margin="0,0,0,10"
            Width="550">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button x:Name="DeleteButton"
                Grid.Row="0"
                Content="✕"
                Width="24"
                Height="24"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                        Click="DeleteButton_Click"/>

                <Button x:Name="DetailsButton"
                Grid.Row="2"
                Content="Class Details"
                Width="110"
                Height="28"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,30,20"
                Cursor="Hand"
                Background="Azure"
                Click="DetailsButton_Click"/>

                <StackPanel Margin="0,0,35,0">

                    <TextBlock x:Name="TitleText"
                       FontSize="20"
                       FontWeight="Bold"
                       Text="Course Title"
                       TextWrapping="Wrap"/>

                    <TextBlock x:Name="DescriptionText"
                       Margin="0,8,0,0"
                       TextWrapping="Wrap"
                       MaxWidth="400"
                       Text="Description"/>

                    <TextBlock x:Name="ProfessorText"
                       Margin="0,8,0,0"
                       Text="Professor"/>

                    <TextBlock x:Name="EmailText"
                       Margin="0,8,0,0"
                       Text="Email"/>

                    <TextBlock x:Name="OfficeHoursText"
                       Margin="0,8,0,0"
                       TextWrapping="Wrap"
                       MaxWidth="400"
                       Text="Office Hours"/>

                    <TextBlock x:Name="ScheduleText"
                       Margin="0,4,0,0"
                       Text="Schedule"/>

                    <TextBlock x:Name="LocationText"
                       Margin="0,4,0,0"
                       Text="Location"/>

                </StackPanel>

            </Grid>

        </Border>

    </Grid>
</UserControl>
